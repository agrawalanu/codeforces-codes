#include<bits/stdc++.h>

using namespace std;

int main()
{
	 int n;
	 cin >> n;
	 
	 int a[n+1];
	 a[0] =0;
	 int sum = n;
	 for(int i =1; i<=n; i++)
	 {
	 	cin >> a[i];
	 	sum+=  abs(a[i]- a[i-1]);
	 }
	 sum += n-1;
	 cout << sum;
}
