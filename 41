#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    
    if(n ==0 || m==0)
	cout << 0;
	else
	{
		int d = min(n,m);
		cout << d+1 << endl;
		for(int i =0; i<=d; i++)
		{
			cout << i << " " << d-i << endl;
		}
		
	} 
}
